# 🚀 部署到 GitHub Pages - 详细步骤

## 第一步：初始化 Git 并提交代码

在项目根目录执行以下命令：

```bash
# 1. 初始化 Git 仓库
git init

# 2. 添加所有文件
git add .

# 3. 创建初始提交
git commit -m "Initial commit: Portfolio website ready for deployment"
```

## 第二步：在 GitHub 上创建仓库

1. 登录 GitHub（如果没有账号，先注册：https://github.com）
2. 点击右上角的 **+** 号，选择 **New repository**
3. 填写仓库信息：
   - **Repository name**: 输入你的仓库名称（例如：`my-portfolio`）
   - **Description**: 可选，填写项目描述
   - **Public/Private**: 选择 Public（GitHub Pages 免费版需要公开仓库）
   - **不要**勾选 "Initialize this repository with a README"（因为本地已有代码）
4. 点击 **Create repository**

## 第三步：连接本地仓库到 GitHub

GitHub 创建仓库后会显示连接命令，类似这样：

```bash
# 添加远程仓库（替换 YOUR_USERNAME 和 YOUR_REPO_NAME）
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

**注意**：将 `YOUR_USERNAME` 替换为你的 GitHub 用户名，`YOUR_REPO_NAME` 替换为你创建的仓库名称。

## 第四步：启用 GitHub Pages

1. 进入你的 GitHub 仓库页面
2. 点击顶部的 **Settings**（设置）标签
3. 在左侧菜单中找到并点击 **Pages**
4. 在 **Source**（源）部分：
   - 选择 **GitHub Actions**（不是 Branch）
5. 页面会自动保存

## 第五步：等待自动部署

1. 点击仓库顶部的 **Actions** 标签
2. 你会看到 "Deploy to GitHub Pages" 工作流正在运行
3. 等待 2-5 分钟，直到看到绿色的 ✓ 标记
4. 部署完成后，在 Actions 页面会显示你的网站链接

## 第六步：访问你的网站

部署完成后，你的网站地址将是：
- **如果仓库名是 `username.github.io`**：`https://username.github.io`
- **如果仓库名是其他名称**：`https://username.github.io/repository-name/`

## ⚠️ 重要提示

### 如果仓库名称不是 `fiona-portfolio`

如果你的仓库名称不同，需要更新 `package.json`：

1. 打开 `package.json`
2. 找到这一行：
   ```json
   "build:github": "parcel build --public-url /fiona-portfolio/"
   ```
3. 将 `/fiona-portfolio/` 改为 `/你的仓库名称/`
4. 然后更新 `.github/workflows/gh-pages.yml`，将：
   ```yaml
   run: npm run build
   ```
   改为：
   ```yaml
   run: npm run build:github
   ```

### 后续更新

以后每次修改代码后，只需要：

```bash
git add .
git commit -m "更新描述"
git push
```

GitHub Actions 会自动重新部署！

## 🆘 遇到问题？

- **部署失败**：检查 Actions 标签页中的错误信息
- **网站无法访问**：确认 Settings → Pages 中选择了 GitHub Actions
- **图片不显示**：等待几分钟让部署完成，或检查浏览器控制台错误

